import React from 'react';

import { YoutubeComponentProps } from '../../component-resolvers/youtube-component-resolver';

export default function Youtube({ children }: YoutubeComponentProps) {
  return <>{children}</>;
}
